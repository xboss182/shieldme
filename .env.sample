# ShieldMe Frontend - environment variables
# VITE_ vars are baked into the bundle at build time.
# Copy to .env before: vite dev  OR  docker build

# Backend API base URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:4005
